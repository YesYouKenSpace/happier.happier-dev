name: Publish Docker Images

on:
  workflow_dispatch:
    inputs:
      channel:
        description: "Release channel"
        required: true
        default: "preview"
        type: choice
        options:
          - dev
          - preview
          - stable
      source_ref:
        description: "Source ref to build from (auto => dev:dev, preview:preview, stable:main)"
        required: true
        default: auto
        type: string
      push_latest:
        description: "Also push the floating channel tag (dev, preview, or latest)"
        required: true
        default: true
        type: boolean
      registries:
        description: "Comma-separated registries to push to (dockerhub,ghcr)"
        required: true
        default: "dockerhub,ghcr"
        type: string
      build_relay:
        description: "Build/push relay-server image"
        required: true
        default: true
        type: boolean
      build_dev_box:
        description: "Build/push dev-box image"
        required: true
        default: true
        type: boolean
  workflow_call:
    inputs:
      channel:
        required: true
        type: string
      source_ref:
        required: false
        default: auto
        type: string
      push_latest:
        required: false
        default: true
        type: boolean
      registries:
        required: false
        default: dockerhub,ghcr
        type: string
      build_relay:
        required: false
        default: true
        type: boolean
      build_dev_box:
        required: false
        default: true
        type: boolean

permissions:
  contents: read
  packages: write

jobs:
  release_actor_guard:
    name: Release actor guard
    permissions:
      contents: read
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Authorize release actor
        uses: ./.github/actions/release-actor-guard
        with:
          team_slug: release-admins
          app_id: ${{ secrets.RELEASE_BOT_APP_ID }}
          private_key: ${{ secrets.RELEASE_BOT_PRIVATE_KEY }}

  publish:
    name: Build and push images
    needs: [release_actor_guard]
    runs-on: ubuntu-latest
    environment: release-shared
    steps:
      - name: Checkout workflow repo
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 1

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22.x

      - name: Resolve channel metadata
        id: channel_meta
        shell: bash
        run: |
          set -euo pipefail
          node scripts/pipeline/release/resolve-public-release-channel-meta.mjs \
            --channel "${{ inputs.channel }}" \
            --source-ref "${{ inputs.source_ref }}" \
            --github-output "$GITHUB_OUTPUT"

      - name: Create GitHub App token
        id: app_token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          app-id: ${{ secrets.RELEASE_BOT_APP_ID }}
          private-key: ${{ secrets.RELEASE_BOT_PRIVATE_KEY }}
          owner: ${{ github.repository_owner }}
          repositories: ${{ github.event.repository.name }}
          permission-contents: read

      - name: Guard stable channel
        if: ${{ inputs.channel == 'stable' && steps.channel_meta.outputs.source_ref != 'main' }}
        run: |
          echo "Stable Docker publishing must build from main (resolved ref: '${{ steps.channel_meta.outputs.source_ref }}')." >&2
          exit 1

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ steps.channel_meta.outputs.source_ref }}
          token: ${{ steps.app_token.outputs.token }}
          persist-credentials: false

      - name: Enable Corepack (Yarn)
        run: |
          corepack enable
          corepack prepare yarn@1.22.22 --activate

      - name: Install dependencies
        env:
          YARN_PRODUCTION: "false"
          npm_config_production: "false"
          HAPPIER_INSTALL_SCOPE: "release-runtime"
          HAPPIER_UI_VENDOR_WEB_ASSETS: "0"
          npm_config_registry: https://registry.npmjs.org
        uses: ./.github/actions/install-yarn-dependencies
        with:
          args: --frozen-lockfile --ignore-engines

      - name: Set up QEMU
        uses: docker/setup-qemu-action@96fe6ef7f33517b61c61be40b68a1882f3264fb8 # v4.2.0

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4.2.0

      - name: Login to Docker Hub
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Login to GHCR
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          registry: ghcr.io
          # Prefer a long-lived PAT when org/package settings prevent GitHub Actions tokens from publishing to GHCR.
          # Fall back to the workflow's GITHUB_TOKEN (packages:write is requested at the workflow level).
          username: ${{ secrets.GHCR_USERNAME != '' && secrets.GHCR_USERNAME || github.actor }}
          password: ${{ secrets.GHCR_PAT != '' && secrets.GHCR_PAT || github.token }}

      - name: Build & push images (pipeline)
        env:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
          GHCR_NAMESPACE: ghcr.io/happier-dev
          GHCR_USERNAME: ${{ secrets.GHCR_USERNAME != '' && secrets.GHCR_USERNAME || github.actor }}
          GHCR_TOKEN: ${{ secrets.GHCR_PAT != '' && secrets.GHCR_PAT || github.token }}
          GH_TOKEN: ${{ steps.app_token.outputs.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          node scripts/pipeline/run.mjs docker-publish \
            --channel "${{ inputs.channel }}" \
            --registries "${{ inputs.registries }}" \
            --push-latest "${{ inputs.push_latest }}" \
            --build-relay "${{ inputs.build_relay }}" \
            --build-dev-box "${{ inputs.build_dev_box }}"

      - name: Update Docker Hub README (relay-server)
        if: ${{ inputs.build_relay && inputs.channel != 'dev' }}
        continue-on-error: true
        uses: peter-evans/dockerhub-description@1b9a80c056b620d92cedb9d9b5a223409c68ddfa # v5.0.0
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
          repository: happierdev/relay-server
          readme-filepath: docker/dockerhub/relay-server.md
          short-description: Self-host the Happier Server (Docker image)

      - name: Update Docker Hub README (dev-box)
        if: ${{ inputs.build_dev_box && inputs.channel != 'dev' }}
        continue-on-error: true
        uses: peter-evans/dockerhub-description@1b9a80c056b620d92cedb9d9b5a223409c68ddfa # v5.0.0
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
          repository: happierdev/dev-box
          readme-filepath: docker/dockerhub/dev-box.md
          short-description: Happier dev box (CLI + daemon in Docker)
